.next/
node_modules/
.env.local
.env
*.log
.DS_Store

# Vídeos originais sem compressão — .mp4 otimizados para web ficam incluídos
*.mov

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Playwright MCP artifacts (snapshots, console logs)
.playwright-mcp/
